# VoxCore Session Handoff — Apr 12-14 2026 (Session 262) — RECONSTRUCTED

> **[RECONSTRUCTED on 2026-05-02]** — This handoff was NOT written contemporaneously at the end of session 262.
> It is a deterministic template fill from the memory files listed in the Sources footer.
> A reader citing facts from this document should cross-verify against the primary sources.
> This is a back-fill so the `AI_Studio/Handoffs/voxcore/` folder has a complete audit trail; it is not a substitute for a real handoff.

**Session:** 262
**Date:** Apr 12-14 2026
**Title:** /ex-refresh --audio + hybrid search 62%→82%
**Commit (best-guess from `git log --grep`):** NOT FOUND
**Source provenance:** see footer

---

## What Happened (from recent-work.md)

- **Full `/ex-refresh --audio`** — audio transcription complete: 52/52 files transcribed (28 new) with faster-whisper medium model on RTX 5090 GPU in 23 min. OCR cached (792/792). Extraction cached (1,483 files). Mbox cached (17,090 msgs, 3,278 attachments).
- **ChromaDB segfault fixed** — multi-segment HNSW data from incremental rebuilds was crashing on `collection.count()`. Nuked and rebuilt clean: 24,930 chunks in single segment, 35 min via Ollama nomic-embed-text at 11.7 chunks/s.
- **Hybrid search quality: 62% → 82%** (41/50, +20 pts from original 31/50). 5 tiers of improvements to `tools/excluded_hybrid_search.py`:
  1. Document-level dedup (collapse chunks per file)
  2. Adaptive FTS boost (2.5x weight when entity density >60%)
  3. Noise folder penalties (OCR images, PayPal statements, archive dupes → 0.3-0.5x)
  4. AND-preferred with OR fallback (AND for precision, OR when AND < 10 results)
  5. FTS5 dash quoting (case IDs like "DI-25-001685" no longer crash FTS5)
- **Additional improvements**: query expansion (acronym dict, 28 military abbreviations), entity boost (25 persons roster from `.cache/persons/persons.json`), cross-chunk document aggregation, larger candidate pools (fts_k 50→150, vec_k 50→100), test suite expanded (6 queries got broader expected_paths).
- **Quality probe reports**: 9 probe runs tracking 31→37→38→39→41/50 progression. Reports in `AI_Studio/Reports/scheduled/quality_probe_20260413_*.{md,json}`.
- **FTS5 standalone: 44% → 60%** (30/50) from OR-mode fallback and dash quoting.
- **LLM reranker built but not usable** — Ollama thinking models (qwen3.5, gemma4) return empty with low num_predict. Needs a non-thinking cross-encoder model (sentence-transformers + CPU PyTorch). Infrastructure ready in code.
- **Remaining 9 failures**: all ranking, no content gaps. Next levers: cross-encoder reranker, semantic chunking (contacts table splitting).
- Commit: `3131b3f418` (combined with session 259 by another tab)



---

## Sources

This reconstructed handoff was generated by `tools/backfill_handoffs.py` on 2026-05-02 from:

- `memory/recent-work.md` lines 150-166 — primary activity log
- git commit: NOT FOUND via `git log --all --grep "session 262"` — session may not have produced a single named commit

To verify any specific claim, open the cited file at the cited line range and read the primary entry.

---

*Reconstructed handoff — DO NOT cite externally without verification against the primary memory files. For going-forward sessions, `/wrap-up` Step 6.5 writes contemporaneous handoffs to this folder automatically.*
