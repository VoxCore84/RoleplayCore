# Follow-up prompt: finish the Claude Code audit v3 (paste into a new tab started at C:\Users\atayl\CalmCore)

You are finishing a Claude Code configuration audit that another tab executed on 2026-09-22. Everything below is the task. Work unattended except where an item says ASK. Do not commit or push.

## Read first (in this order, nothing else before starting)
1. `C:/Users/atayl/VoxCore/AI_Studio/Handoffs/calmcore/2026-09-22_cc_audit_v3_calmcore.md` (your main work list)
2. `C:/Users/atayl/VoxCore/AI_Studio/Reports/cc_audit_v3_20260922-1804/REPORT.md` (what was done, with labels)
3. `C:/Users/atayl/VoxCore/AI_Studio/Reports/cc_audit_v3_20260922-1804/APPLIED_LEDGER.md` (backup and rollback convention: `<file>.bak_<YYYYMMDD_HHMMSS>_<reason>` beside untracked files; textual edits only, never a `json.dump` round-trip on git-tracked JSON)
4. `C:/Users/atayl/VoxCore/tasks/windows-shell-checklist.md` (traps that cost time last night: `pythonw` and `Start-Process -Wait` hold the calling tool; run slash commands from the PowerShell tool; heredoc bodies that quote destructive commands may be blocked by hooks, so write such text with the Write/Edit tool)

## Adam's answers (fill in before pasting; blank = the default shown)
- DEFER_REMAINING_MCP: yes | no   (default yes: measured 57.8k -> 52.0k live tokens per turn; cost is one tool-search round trip the first time arcanum, docs-rag or local-llm is used in a session; no answer-quality effect)
- ASK_TIMEOUT: yes | no   (default yes: adds `"askUserQuestionTimeout": "10m"` so an unattended run continues on Claude's own judgment after 10 minutes instead of waiting forever; the risk is that a question you wanted to answer gets auto-continued)
- DEFENDER_DONE: yes | no   (default no: if yes, Adam already ran the three Add-MpPreference lines from the handoff as admin; if no, just re-list them in the report)

## Do, in this order
1. **CalmCore items 1 to 5, 7 and 8 from the handoff.** Register the `git-destructive-guard` route, the async Stop `--ensure`, and the `if` filter on `sync-on-git` in CalmCore's hooks; first determine whether CalmCore's hook blocks in `.claude/settings.json` AND `.claude/settings.local.json` both fire (every hook would run twice) and report that before changing anything; rename `.claude/commands/status.md` to `sys-status.md`; run the guard route matrix `python C:/Users/atayl/VoxCore/AI_Studio/Reports/cc_audit_v3_20260922-1804/guard_smoke.py` and the two safe real-path tests (a `rm -rf _scratch/...` probe, a `mysql ... world < /dev/null` string); do the clangd LSP test (deliberate type error in a scratch `.cpp`, diagnostic arrives, revert; one definition lookup via the LSP tool and one via the `codeintel` MCP server, record calls and latency); prune `enabledMcpjsonServers` in `CalmCore/.claude/settings.local.json` to the servers that actually connect per `claude mcp list`; run `/sync-infra` for the parity deltas and list what it found. The daemon on port 19484 is shared with VoxCore and already runs v1.3.1; do not restart it unless you change `hook_daemon.py`, and if you must, restart from the PowerShell tool with `Start-Process pythonw -ArgumentList 'C:\Users\atayl\VoxCore\.claude\hooks\daemon_shim.py' -WindowStyle Hidden` and poll `/health` in a separate call.
2. **If DEFER_REMAINING_MCP = yes:** back up `C:/Users/atayl/VoxCore/.mcp.json` beside itself, delete the three remaining `"alwaysLoad": true,` lines (arcanum, docs-rag, local-llm), validate JSON, then from the PowerShell tool run `python C:/Users/atayl/VoxCore/tools/cc_context_capture.py --label v3_followup --diff C:/Users/atayl/VoxCore/AI_Studio/Reports/context_captures/20260922_190535_v3_after.md` with the VoxCore directory as cwd and record the total (expect about 52.0k; report whatever it is).
3. **If ASK_TIMEOUT = yes:** back up `~/.claude/settings.json` beside itself and add the line `"askUserQuestionTimeout": "10m",` directly under `"fastModePerSessionOptIn": true,` as a text edit; validate JSON; confirm the key with `python C:/Users/atayl/VoxCore/tools/cc_env_check.py --also askUserQuestionTimeout`.
4. **USER-RUN checks (ASK).** Print this list and ask Adam to type each in a fresh VoxCore session and paste the results; record them next to the expected values from REPORT § Verification: `/status`, `/model` (does Fable show "Requires usage credits"?), `/effort status`, `/hooks`, `/mcp`, `/permissions`, `/plugin`, `/usage` (5-hour and 7-day bars). If Adam declines, list them as still USER-RUN and move on.
5. **Close out.** Append your rows to `APPLIED_LEDGER.md` (continue the numbering from 31), add a "Follow-up 2026-09-2x" section to `REPORT.md` with every item labelled FUNCTIONALLY VERIFIED / MEASURED / EXPECTED-NOT-MEASURED / UNAVAILABLE / BLOCKED / USER-RUN, update the dated line in `~/.claude/projects/C--Users-atayl-VoxCore/memory/claude-code-config-state.md` (supersede, do not rewrite), refresh the "Latest session" line in `C:/Users/atayl/VoxCore/docs/VOXCORE_HANDOFF_INDEX.md`, and write `C:/Users/atayl/VoxCore/AI_Studio/Handoffs/voxcore/<date>_session_cc_audit_v3_followup.md` (phases table, metrics deltas, what is queued, debt, honest assessment). Then say what is still open and suggest `/wrap-up` for the commit decision.

## Still open for Adam, not for you
Commit strategy (VoxCore `CLAUDE.md` carries 31 pre-existing uncommitted lines plus the audit's dedupe; separate diffs are in the audit folder); the 28 `tools/` scripts still on Claude 4.x model IDs (needs a GO); the four past-due entries and the Section 1983 deadline in `.claude/deadlines.json`; whether to demote three audit-trail rules to `paths:` scoping (measured -4.7k, recommended against because they would be absent on the first write of a closeout).

## Do not
Commit or push; run `git reset --hard`, `git clean`, or a force push (the guard will block them and it is right to); touch `Desktop/Excluded`, `Desktop/IMPORTANT DOCS`, `.claude/rules/excluded-corpus.md`, `.claude/deadlines.json`, or any case/career/finance memory file; edit production code under `tools/`; use `json.dump` on git-tracked files; run a slash command through the Bash tool; claim a token saving without a `cc_context_capture.py` total; switch the model or effort (Fable 5.1 at xhigh is a standing decision, `memory/feedback_power_over_efficiency.md`).
