# VoxCore Environment Manifest

**Generated:** 2026-05-02
**Scope:** Records the build / runtime environment for VoxCore as of separation-prep diligence. Refreshed any time a major component changes (Python upgrade, GPU swap, OS reinstall).

This is the canonical answer to "what does it take to reproduce VoxCore on a clean machine?" — required reading for IP chain-of-title diligence (Cat 7) and operational readiness (Cat 8).

---

## Hardware

| Component | Value |
|-----------|-------|
| **CPU** | AMD Ryzen 9 9950X3D (16 cores, 32 threads) |
| **RAM** | 128 GB DDR5 |
| **GPU** | NVIDIA GeForce RTX 5090, 32 GB VRAM (32607 MiB) |
| **GPU driver** | 596.21 |
| **Storage** | NVMe (primary), additional secondary drives for archive |
| **Network** | Personal home internet, no .mil VPN |

All hardware **personally owned**. Receipts and serial numbers cataloged in the IP chain-of-title folder (`03_IP_Chain_of_Title/01_Hardware/`).

---

## Operating System

| Component | Value |
|-----------|-------|
| **OS** | Microsoft Windows 11 Pro |
| **Build** | 10.0.26200 |
| **Edition** | Pro |
| **Architecture** | AMD64 |

PowerShell 7 (`pwsh`) is the primary shell. Bash also available via Git Bash for POSIX scripts.

---

## Python Runtime

| Component | Value |
|-----------|-------|
| **Python** | 3.14.3 (system-installed at `C:\Python314\python.exe`) |
| **pip** | 26.0.1 |
| **Virtualenvs** | Not used — system Python with global site-packages |

To re-create on a clean machine: install Python 3.14.3 from python.org, install dependencies from each `requirements.txt` (or the `.pinned.txt` variants generated by `tools/deps_audit.py --fix` for exact reproducibility).

---

## Key Python Packages (current installed versions)

| Package | Version | Notes |
|---------|---------|-------|
| `pdfplumber` | 0.11.9 | PDF text/layout (replaces PyMuPDF/AGPL) |
| `pypdfium2` | 5.7.0 | PDF rendering (Apache 2.0) |
| `pdf2image` | 1.17.0 | Poppler-based render (BSD) |
| `pillow` | 12.1.1 | Image library |
| `pymysql` | 1.1.2 | MySQL driver (replaces mysql-connector-python/GPL) |
| `chromadb` | 1.5.7 | Vector store |
| `anthropic` | 0.84.0 | Claude SDK |
| `openai` | 2.26.0 | OpenAI/Codex SDK |
| `mcp` | 1.26.0 | Model Context Protocol |

Full per-project dependency lists live in `tools/*/requirements.txt`. Reproducible `.pinned.txt` variants generated by `python tools/deps_audit.py --fix`.

---

## Local AI Stack (Ollama)

| Component | Value |
|-----------|-------|
| **Endpoint** | `http://localhost:11434` (default Ollama) |
| **Model: judge / classifier** | `gemma4:26b` (default for `citation_scorer.py --judge ollama`) |
| **Model: reasoning / fallback** | `qwen3.5:27b-q4_K_M` |
| **Model: embeddings (dense)** | `bge-large:latest` (1024 dim) and `snowflake-arctic-embed:335m` |
| **Model: reranker** | `qllama/bge-reranker-v2-m3:latest` |
| **Model: lightweight embed** | `nomic-embed-text:latest` (768 dim) |

Local GPU offload pattern: privilege-sensitive operations (embeddings, reranking, OCR via Tesseract, ASR via Whisper) run on the RTX 5090; cloud APIs (Anthropic, OpenAI, Google) are reserved for non-privileged orchestration.

---

## Databases

| DB | Path / Endpoint | Purpose |
|----|-----------------|---------|
| **FTS5 corpus index** | `.cache/excluded_fts.db` | 20,807 chunks across mbox/PDF/DOCX/audio transcripts |
| **Knowledge graph** | `.cache/excluded_kg.db` | 24,640 entities, 175,793 mentions, 743,207 relations |
| **ChromaDB vector store** | `.cache/chroma_excluded/` | 24,930 embeddings |
| **Mbox index** | `.cache/excluded_daemon/mbox_index.db` | 17,090 messages, 3,278 attachments |
| **MySQL** | `localhost:3306`, root/admin | UniServerZ 9.5.0 bundled, 5 schemas (auth/characters/world/hotfixes/roleplay) — **CalmCore (WoW) only**, not VoxCore |

VoxCore's data layer is SQLite-only. The MySQL stack is shared infrastructure inherited from the CalmCore project.

---

## CUDA / GPU

| Component | Value |
|-----------|-------|
| **CUDA toolkit** | Not installed at the system level (no `nvcc`); CUDA runtime ships with PyTorch / Ollama bundled drivers |
| **NVIDIA driver** | 596.21 |
| **GPU mode** | Compute (default), NO virtualization, NO multi-instance GPU |

For development, GPU access flows through PyTorch / Ollama / pypdfium2 — the system-level CUDA toolkit isn't required.

---

## External Tools

| Tool | Path | Purpose |
|------|------|---------|
| **Tesseract OCR** | (system PATH) | Used by `tools/unredact/ocr.py` for scanned PDF OCR |
| **ffmpeg** | (system PATH) | Audio transcoding for Whisper |
| **Git** | `C:\Program Files\Git\` | VCS — used for IP chain-of-title timeline analysis |
| **Ollama** | (Windows service) | Local LLM runtime |

---

## Reproducibility Status

| Item | Status |
|------|--------|
| Hardware personally owned | ✅ Receipts cataloged |
| OS personally licensed | ✅ Win 11 Pro retail license |
| Python deps tracked | ✅ 7 `requirements.txt` files; ⚠ unpinned (use `.pinned.txt` for exact repro — see `tools/deps_audit.py`) |
| AI subscriptions personal | ✅ Audit in progress (Cat 7 #6) |
| GPU drivers personal install | ✅ NVIDIA App / driver download |
| No government-furnished equipment | ✅ Confirmed — all hardware personal |
| No .mil network used | ✅ Confirmed — home internet only |

---

## Refresh Triggers

Update this file when:
- Python major/minor version changes (e.g. 3.14 → 3.15)
- GPU swap or driver major update
- New external tool added to the dev stack
- OS reinstall or major Windows feature update
- Any change to the local AI stack (Ollama models, endpoint port)

When refreshed, increment the **Generated** date at top.

---

*This file is a diligence artifact. Acquirer technical-diligence teams will compare this against the actual repo state. Keep it current.*
